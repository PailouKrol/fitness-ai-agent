# FitVision AI - Telegram фитнес-агент

🤖 Telegram бот для анализа питания и фигуры по фото с использованием AI.

## Возможности
- 📸 Анализ еды по фото (калории, БЖУ)
- 🏋️ Анализ фигуры по фото (не медицинский!)
- 📊 Отслеживание прогресса веса
- 💪 Персональные рекомендации
- 📈 Статистика в веб-интерфейсе

## Технологии
- Python 3.10+
- FastAPI + Uvicorn
- pyTelegramBotAPI
- OpenAI GPT-4o-mini
- SQLite
- AdminLTE 3

## Установка
1. Клонировать репозиторий
2. Создать виртуальное окружение
3. Установить зависимости: `pip install -r requirements.txt`
4. Создать `.env` файл с токенами
5. Запустить: `python main.py`

## 🖥️ Админ-панель

Веб-интерфейс для администратора находится в папке [`/admin_panel`](./admin_panel):

- `login.html` — страница входа
- `stats.html` — статистика пользователей
- `user_stats.html` — детальный просмотр пользователя
- `users.json` — файл с логинами администраторов (по умолчанию admin/admin)

**Важно:** в коде бота (`main.py`) прописан путь к этим файлам:
```python
templates = Jinja2Templates(directory="/var/www/dmtr.fvds.ru")


## Тестирование
```bash
python -m unittest discover tests -v
